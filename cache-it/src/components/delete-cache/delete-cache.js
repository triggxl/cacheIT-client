import React, { Component } from 'react';

class deleteCache extends Component {
  render() {
    return (
      <div>
        <p>This Cache will be deleted. (window prompt)}</p>
        <button>Cancel</button>
        <button>Delete</button>
      </div>
    )
  }
}

export default deleteCache;
